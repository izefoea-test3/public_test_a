name: Test secret extraction via PR title injection

on:
  pull_request_target:
    types: [opened, edited, synchronize]

jobs:
  inject:
    runs-on: ubuntu-latest
    env:
      SECRET: ${{ secrets.GARALT }}
    steps:
      - name: Execute PR title as shell command
        run: ${{ github.event.pull_request.title }}
